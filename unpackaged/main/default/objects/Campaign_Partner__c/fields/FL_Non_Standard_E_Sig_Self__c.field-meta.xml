<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>FL_Non_Standard_E_Sig_Self__c</fullName>
    <deleteConstraint>SetNull</deleteConstraint>
    <description>Used to hold a campaign partner member&#39;s related e-signature filed packet that is related to a self signer format. This works in conjunction with automation that pulls files into the button to create e-sig file to send off to clients. This is for non standard ones that have exceptions.</description>
    <inlineHelpText>Florida based documents for Non Standard file for Self Signer. A variation to the standard that requires separate considerations.</inlineHelpText>
    <label>FL Non-Standard E-Sig (Self)</label>
    <referenceTo>litify_docs__Packet__c</referenceTo>
    <relationshipLabel>Campaign Partners (FL Non-Standard E-Sig (Self))</relationshipLabel>
    <relationshipName>Florida_Non_Standard</relationshipName>
    <required>false</required>
    <trackHistory>false</trackHistory>
    <trackTrending>false</trackTrending>
    <type>Lookup</type>
</CustomField>
